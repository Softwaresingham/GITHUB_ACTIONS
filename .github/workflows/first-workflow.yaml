name: list all the software

on:
  push

jobs:
  list:
    runs-on: ubuntu-latest

    steps:
      - name: List installed software
        run: |
          mvn -version
          npm -v
          docker --version
          kubectl version --client
