name: Install Java Without Action

on:
  push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - name: Install Java
        run: |
          sudo apt update
          sudo apt install openjdk-17-jdk -y

      - name: Verify Java
        run: java -version
        
