name: events-pipeline
on: 
  - push:
  - pull_request:
  - schedule:
      - cron: '0 0 * * *' 
  - workflow_dispatch:
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo event
        run: run: echo "triggred using a(n) ${{github.event_name}} event"

