name: runners

on:
  push

jobs:
  print-ubuntu:
    runs-on: ubuntu-latest

    steps:
      - name: Show Message
        run: echo "This is running on Ubuntu"

      - name: Show Runner OS
        run: echo "RUNNER OS: $RUNNER_OS"
