name: npm install
on:
  push
jobs:
  npm-install:
    runs-on: ubuntu-latest
    steps:
      - name: NPM or Yarn install with caching
        uses: bahmutov/npm-install@v1.12.1 
      - name: version
        run: npm -v
